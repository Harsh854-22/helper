
// AI Configuration

// Env variables for AI settings if needed
export const env = {
  // Add any configuration options here if needed in the future
  // This serves as a central place for AI-related configuration
  maxResponseLength: 500,
  defaultTemperature: 0.7,
  maxContextLength: 5
};
